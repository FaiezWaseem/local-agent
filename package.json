{
    "name": "deepseek-local-agent",
    "private": true,
    "workspaces": [
        "daemon",
        "extension"
    ],
    "scripts": {
        "build": "npm -w daemon run build && npm -w extension run build",
        "build:exe": "npm -w daemon run build:exe",
        "dev:daemon": "npm -w daemon run dev",
        "start": "npm -w daemon start",
        "test": "npm -w daemon run build && npm -w daemon run test:completion && npm -w extension run test:parser && npm -w extension run test:streams"
    }
}
