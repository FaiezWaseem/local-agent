import Fastify from 'fastify';import cors from '@fastify/cors';import path from 'node:path';import fs from 'node:fs';import os from 'node:os';import crypto from 'node:crypto';import {fsTool} from './tools/filesystem.js';import {shell} from './tools/shell.js';import {git} from './tools/git.js';import {log,history} from './db/history.js';
const app=Fastify({logger:true});await app.register(cors,{origin:[/^chrome-extension:\/\//,'https://chat.deepseek.com','https://chat.qwen.ai','https://chat.z.ai']});const cfgDir=path.join(os.homedir(),'.deepseek-local');fs.mkdirSync(cfgDir,{recursive:true});const tokenFile=path.join(cfgDir,'token');let TOKEN=process.env.DEEPSEEK_LOCAL_TOKEN||'';if(!TOKEN){if(fs.existsSync(tokenFile))TOKEN=fs.readFileSync(tokenFile,'utf8').trim();else{TOKEN=crypto.randomBytes(24).toString('hex');fs.writeFileSync(tokenFile,TOKEN,{mode:0o600})}}let workspace=path.resolve(process.env.DEEPSEEK_WORKSPACE||process.cwd());const auth=(req:any,rep:any)=>{if(req.headers.authorization!==`Bearer ${TOKEN}`){rep.code(401).send({error:'unauthorized'});return false}return true};app.get('/health',async()=>({ok:true,workspace}));app.post('/connect',async(req:any,rep)=>{if(req.body?.token!==TOKEN)return rep.code(401).send({error:'bad token'});return {ok:true,workspace}});app.post('/workspace',async(req:any,rep)=>{if(!auth(req,rep))return;const p=path.resolve(req.body.path);if(!fs.existsSync(p)||!fs.statSync(p).isDirectory())return rep.code(400).send({error:'workspace must be an existing directory'});workspace=p;return {ok:true,workspace}});app.get('/history',async(req:any,rep)=>{if(!auth(req,rep))return;return {items:history(50)}});app.post('/tool',async(req:any,rep)=>{if(!auth(req,rep))return;const {name,arguments:a={}}=req.body||{};try{let result;if(['read_file','write_file','edit_file','delete_file','list_directory'].includes(name))result=await fsTool(workspace,name,a);else if(name==='run_command')result=await shell(workspace,a);else if(['git_status','git_diff','git_log'].includes(name))result=await git(workspace,name,a);else throw new Error('Unknown tool: '+name);log(name,a,result,true);return {success:true,result}}catch(e:any){log(name,a,{error:e.message},false);return {success:false,error:e.message}}});app.listen({host:'127.0.0.1',port:Number(process.env.PORT||43121)}).then(()=>console.log(`\nLocal AI Agent ready\nWorkspace: ${workspace}\nPairing token: ${TOKEN}\n`));
