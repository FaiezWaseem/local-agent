import Database from 'better-sqlite3';import os from 'node:os';import path from 'node:path';import fs from 'node:fs';
const dir=path.join(os.homedir(),'.deepseek-local');fs.mkdirSync(dir,{recursive:true});const db=new Database(path.join(dir,'history.db'));db.exec(`CREATE TABLE IF NOT EXISTS calls(id INTEGER PRIMARY KEY,ts TEXT,tool TEXT,args TEXT,result TEXT,ok INTEGER);`);export function log(tool:string,args:any,result:any,ok:boolean){db.prepare('INSERT INTO calls(ts,tool,args,result,ok) VALUES(?,?,?,?,?)').run(new Date().toISOString(),tool,JSON.stringify(args),JSON.stringify(result),ok?1:0)}export function history(limit=50){return db.prepare('SELECT * FROM calls ORDER BY id DESC LIMIT ?').all(limit)}